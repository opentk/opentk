namespace OpenTK.Platform
{
    /// <summary>
    /// Enumeration of surface types.
    /// </summary>
    public enum SurfaceType
    {
        /// <summary>
        /// A type of surface which spans an entire display.
        /// </summary>
        Display,

        /// <summary>
        /// A type of surface which is embedded in a container.
        /// </summary>
        Control
    }
}
