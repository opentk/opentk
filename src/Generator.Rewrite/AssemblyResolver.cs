using Mono.Cecil;

namespace OpenTK.Rewrite
{
    /// <summary>
    /// A class that is responsible for resolving assembly references.<para/>
    /// Note that this is currently exactly the same as the <see cref="BaseAssemblyResolver"/> because there is no need
    /// for any additional functionality.
    /// </summary>
    public class AssemblyResolver : BaseAssemblyResolver
    {
    }
}
